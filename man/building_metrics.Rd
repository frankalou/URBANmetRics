% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/building_metrics.R
\name{building_metrics}
\alias{building_metrics}
\title{Compute Building Metrics for an AOI}
\usage{
building_metrics(building_result, aoi)
}
\arguments{
\item{building_result}{An \code{sf} object returned by \code{building_structure()},
containing building features with a \code{category} column.}

\item{aoi}{An \code{sf} polygon representing the Area of Interest.
Must be a valid \code{sf} object.}
}
\value{
A named list containing:
\describe{
\item{AOI}{AOI area in m² and km²}
\item{summary}{Total building area and its share of the AOI}
\item{categories}{A data frame of building metrics per category
returned by \code{osm_metrics()}}
}
}
\description{
This function is a wrapper around \code{osm_metrics()} and processes the output
of \code{building_structure()}. It calculates geometry-based building metrics
per category and provides a structured summary for downstream analysis
within the URBANmetRics workflow.
}
\details{
The function:
\itemize{
\item projects the AOI and building features to a metric CRS (EPSG:3857),
\item computes category-level metrics using \code{osm_metrics()},
\item calculates total building area and its share of the AOI,
\item returns a structured list containing AOI information, summary statistics,
and category-level metrics.
}

This function provides the building-specific metrics layer of the
URBANmetRics workflow. It does not classify buildings (this is handled in
\code{building_structure()}), but instead computes geometric and density-based
metrics for each building category.

The modular design ensures:
\itemize{
\item consistent metric definitions across domains,
\item reproducibility,
\item separation of concerns between structure extraction and metric computation.
}
}
\examples{
\dontrun{
buildings <- building_structure(aoi)
metrics <- building_metrics(buildings, aoi)
metrics$summary
}

}
