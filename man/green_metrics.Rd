% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/green_metrics.R
\name{green_metrics}
\alias{green_metrics}
\title{Compute Green Space Metrics for an AOI}
\usage{
green_metrics(green_result, aoi)
}
\arguments{
\item{green_result}{An \code{sf} object returned by \code{green_structure()},
containing green-space features with a \code{category} column.}

\item{aoi}{An \code{sf} polygon representing the Area of Interest.
Must be a valid \code{sf} object.}
}
\value{
A named list containing:
\describe{
\item{AOI}{AOI area in m² and km²}
\item{summary}{Total green area and its share of the AOI}
\item{categories}{A data frame of green-space metrics per category
returned by \code{osm_metrics()}}
}
}
\description{
This function is a wrapper around \code{osm_metrics()} and processes the output
of \code{green_structure()}. It calculates geometry-based green-space metrics
per category and provides a structured summary for downstream analysis
within the URBANmetRics workflow.
}
\details{
The function:
\itemize{
\item projects the AOI and green features to a metric CRS (EPSG:3857),
\item computes category-level metrics using \code{osm_metrics()},
\item calculates total green area and its share of the AOI,
\item returns a structured list containing AOI information, summary statistics,
and category-level metrics.
}

This function provides the green-specific metrics layer of the
URBANmetRics workflow. It does not classify green features (this is handled
in \code{green_structure()}), but instead computes geometric and density-based
metrics for each green-space category.

The modular design ensures:
\itemize{
\item consistent metric definitions across domains,
\item reproducibility,
\item separation of concerns between structure extraction and metric computation
}
}
\examples{
\dontrun{
greens <- green_structure(aoi)
metrics <- green_metrics(greens, aoi)
metrics$summary
}

}
