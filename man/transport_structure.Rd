% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transport_structure.R
\name{transport_structure}
\alias{transport_structure}
\title{Extract Transport Features for an AOI Using Predefined OSM Tags}
\usage{
transport_structure(aoi)
}
\arguments{
\item{aoi}{An \code{sf} polygon representing the Area of Interest.
Must be a valid \code{sf} object.}
}
\value{
An \code{sf} object containing transport features intersecting the AOI,
including:
\describe{
\item{geometry}{Spatial geometry of each transport feature}
\item{transport_class}{Assigned transport class from \code{classify_transport()}}
\item{category}{Alias for \code{transport_class}, used for consistency}
\item{area}{Feature area (if polygonal)}
\item{length}{Feature length (for linear features)}
\item{count}{Feature count (always 1 per row)}
\item{all original OSM attributes}{All OSM key–value pairs returned by Overpass}
}
}
\description{
This function is a wrapper around \code{osm_structure()} and provides a
predefined set of OSM transport-related tags (\code{transport_tags}). It represents
the transport-specific entry point of the URBANmetRics workflow. The function
retrieves all OSM transport features within the Area of Interest (AOI),
harmonizes them through the core extraction engine, and applies a transport
classification routine.
}
\details{
The function:
\itemize{
\item calls \code{osm_structure()} with the predefined \code{transport_tags},
\item validates and filters the returned OSM features,
\item applies \code{classify_transport()} to assign transport classes,
\item returns a clean \code{sf} object with transport categories and metrics.
}

This function is not intended to be used as a standalone OSM extractor.
Instead, it provides a standardized transport-specific wrapper around
\code{osm_structure()}. The modular design ensures:
\itemize{
\item consistent preprocessing across thematic domains,
\item reproducible transport classification,
\item separation of concerns between extraction and interpretation
}
}
\examples{
\dontrun{
transport <- transport_structure(aoi)
plot(transport["transport_class"])
}

}
