% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biulding_structure.R
\name{building_structure}
\alias{building_structure}
\title{Extract Building Features for an AOI Using Predefined OSM Tags}
\usage{
building_structure(aoi)
}
\arguments{
\item{aoi}{An \code{sf} polygon representing the Area of Interest.
Must be a valid \code{sf} object.}
}
\value{
An \code{sf} object containing building features intersecting the AOI,
including:
\describe{
\item{geometry}{Spatial geometry of each building feature}
\item{building_class}{Assigned building class from \code{classify_buildings()}}
\item{category}{Alias for \code{building_class}, used for consistency}
\item{area}{Feature area (if polygonal)}
\item{length}{Feature length (if linear)}
\item{count}{Feature count (always 1 per row)}
\item{all original OSM attributes}{All OSM key–value pairs returned by Overpass}
}
}
\description{
This function is a wrapper around \code{osm_structure()} and provides a
predefined set of OSM building tags (\code{building_tags}). It represents the
building-specific entry point of the URBANmetRics workflow. The function
retrieves all OSM building features within the Area of Interest (AOI),
harmonizes them through the core extraction engine, and applies a
building classification routine.
}
\details{
The function:
\itemize{
\item calls \code{osm_structure()} with the predefined \code{building_tags},
\item filters and validates the returned OSM features,
\item applies \code{classify_buildings()} to assign building classes,
\item returns a clean \code{sf} object with building categories and metrics.
}

This function is not intended to be used as a standalone OSM extractor.
Instead, it provides a standardized building-specific wrapper around
\code{osm_structure()}. The modular design ensures:
\itemize{
\item consistent preprocessing across thematic domains,
\item reproducible building classification,
\item separation of concerns between extraction and interpretation.
}
}
\examples{
\dontrun{
buildings <- building_structure(aoi)
plot(buildings["building_class"])
}

}
