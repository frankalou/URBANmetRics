% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/green_structure.R
\name{green_structure}
\alias{green_structure}
\title{Extract Green Space Features for an AOI Using Predefined OSM Tags}
\usage{
green_structure(aoi)
}
\arguments{
\item{aoi}{An \code{sf} polygon representing the Area of Interest.
Must be a valid \code{sf} object.}
}
\value{
An \code{sf} object containing green-space features intersecting the AOI,
including:
\describe{
\item{geometry}{Spatial geometry of each green feature}
\item{category}{Assigned green-space category based on \code{green_tags}}
\item{area}{Feature area (for polygons)}
\item{length}{Feature length (for linear features)}
\item{count}{Feature count (always 1 per row)}
\item{all original OSM attributes}{All OSM key–value pairs returned by Overpass}
}
}
\description{
This function is a wrapper around \code{osm_structure()} and provides a
predefined set of OSM green-space tags (\code{green_tags}). It represents the
green-specific entry point of the URBANmetRics workflow. The function
retrieves all OSM green features within the Area of Interest (AOI),
harmonizes them through the core extraction engine, and prepares them
for downstream metric calculations.
}
\details{
The function:
\itemize{
\item calls \code{osm_structure()} with the predefined \code{green_tags},
\item validates and filters the returned OSM features,
\item returns a clean \code{sf} object with green-space categories and metrics.
}

This function is not intended to be used as a standalone OSM extractor.
Instead, it provides a standardized green-specific wrapper around
\code{osm_structure()}. The modular design ensures:
\itemize{
\item consistent preprocessing across thematic domains,
\item reproducible green-space classification,
\item separation of concerns between extraction and interpretation.
}
}
\examples{
\dontrun{
greens <- green_structure(aoi)
plot(greens["category"])
}

}
