% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osm_structure.R
\name{osm_structure}
\alias{osm_structure}
\title{Query and Prepare OSM Features for a Given AOI}
\usage{
osm_structure(aoi, tags)
}
\arguments{
\item{aoi}{An \code{sf} polygon representing the Area of Interest.
Must be a valid \code{sf} object.}

\item{tags}{A named list defining OSM key–value pairs to extract.
Each list element corresponds to an OSM key. Values may be:
\itemize{
\item \code{TRUE} to match all values of that key, or
\item a character vector of specific OSM tag values.
}}
}
\value{
An \code{sf} object containing all OSM features that match the tag specification
and intersect the AOI. The returned object includes:
\describe{
\item{geometry}{Spatial geometry (POINT, LINESTRING, POLYGON, etc.)}
\item{category}{Assigned category based on the tag list}
\item{area}{Feature area (for polygons)}
\item{length}{Feature length (for lines)}
\item{count}{Feature count (always 1 per row)}
\item{all original OSM attributes}{(e.g., \code{building}, \code{landuse}, \code{highway}, etc.)}
}
}
\description{
This function queries OpenStreetMap (OSM) features for a given Area of Interest (AOI)
using a set of user-defined OSM tag specifications. It serves as the \emph{core extraction
engine} of the URBANmetRics workflow. Wrapper functions such as \code{building_structure()},
\code{green_structure()}, and \code{transport_structure()} rely on this function to retrieve and
preprocess OSM data for specific thematic domains.
}
\details{
The function:
\itemize{
\item queries multiple Overpass API fallback servers,
\item retrieves all matching geometries (points, lines, polygons, multipolygons),
\item harmonizes attribute columns across geometry types,
\item repairs invalid geometries,
\item casts geometry types where necessary (e.g., MULTILINESTRING → LINESTRING),
\item computes basic metrics (area, length, count),
\item assigns feature categories based on the provided tag list,
\item filters results to the AOI.
}

This function is intentionally designed as a \emph{generic OSM extraction framework}.
It does not classify features by itself. Instead, wrapper functions provide
thematic tag sets and post-processing logic. This modular design ensures:
\itemize{
\item reproducibility,
\item extensibility,
\item consistent preprocessing across all thematic domains.
}
}
\examples{
\dontrun{
# Example: extract all buildings in an AOI
tags <- list(building = TRUE)
buildings <- osm_structure(aoi, tags)
}

}
