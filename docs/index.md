Provides a modular workflow to extract urban structures (buildings,
green spaces, transport infrastructure) from OpenStreetMap, compute
geometry-based metrics, and combine them into an integrated urban index.
The package currently includes structure extraction, classification,
metric computation, and index calculation.
